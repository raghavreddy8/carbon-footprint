/**
 * @file test-setup.ts
 * @description Global test setup file. Extends Vitest's expect with jest-dom matchers
 * such as toBeInTheDocument(), toHaveStyle(), and toBeDisabled() for more expressive
 * and idiomatic React Testing Library assertions.
 */
import '@testing-library/jest-dom';
